#include <iostream>

using namespace std;

void playGame()
{
    //declaring variables
    const int mapSize = 9;
    int numberOfBombs = 10;
    srand((unsigned)time(0));
    char map[mapSize][mapSize];
    char visibleMap[mapSize][mapSize];
}

int main()
{
    playGame();

    return 0;
}